export enum ApiUrl {
  USER = "/user",
  SEARCH = "/search",
}

export enum HeaderKey {
  CONTENT_TYPE = "Content-Type",
  X_SERVICE = "X-Service",
}

export enum ContentType {
  JSON = "application/json",
}

export enum XService {
  SEARCH = "search",
  USER = "user",
}
